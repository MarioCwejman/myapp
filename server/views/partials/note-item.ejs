<div id="note-<%= note.id %>" style="border:1px solid #ccc; padding:8px; margin:8px 0;">
  <strong><%= note.title %></strong>
  <div><%= note.body %></div>
  <small>id: <%= note.id %></small>

  <!-- Delete button (HTMX DELETE) -->
</div>   
<div class="note">
  ...
  <button
    type="button"
    hx-delete="/notes/<%= note.id %>"
    hx-target="closest .note"
    hx-swap="outerHTML"
  >Delete</button>
</div>

